//ESTE ARCHIVO NO SE UTILIZA, ES SOLO PARA GUARDAR COMO SE HARÍA EN UNA BASE DE DATOS
/*   await mongoose.connect(
    'mongodb+srv://RTC-scrapper:YeTkFlylr40C5pgi@cluster0.czmyo.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0'
  );

  const allPortatiles = await Portatil.find();
  if (allPortatiles.length) {
    await Portatil.collection.drop();
    console.log('Éxito borrando datos');
  }

  await Portatil.insertMany(PORTATILES);
  await mongoose.disconnect();*/
